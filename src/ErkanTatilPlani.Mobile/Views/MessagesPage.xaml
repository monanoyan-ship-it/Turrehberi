<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ErkanTatilPlani.Mobile.ViewModels"
             x:Class="ErkanTatilPlani.Mobile.Views.MessagesPage"
             Title="Mesajlar">

    <ContentPage.BindingContext>
        <viewmodels:MessagesViewModel />
    </ContentPage.BindingContext>

    <RefreshView IsRefreshing="{Binding IsBusy}" Command="{Binding RefreshCommand}">
        <CollectionView ItemsSource="{Binding Conversations}" EmptyView="Henuz mesajiniz yok">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:ConversationItem">
                    <Frame Margin="10,5" Padding="15" CornerRadius="10" HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                            <Label Text="{Binding OtherPartyName}" FontSize="16" FontAttributes="Bold" />
                            <Label Grid.Column="1" Text="{Binding LastMessageDate}" TextColor="Gray" FontSize="12" />
                            <Label Grid.Row="1" Text="{Binding LastMessage}" TextColor="Gray" MaxLines="1" />
                            <Frame Grid.Row="1" Grid.Column="1" Padding="6,2" CornerRadius="10"
                                   BackgroundColor="#1976D2" HasShadow="False"
                                   IsVisible="{Binding HasUnread}" VerticalOptions="Center">
                                <Label Text="{Binding UnreadCount}" TextColor="White" FontSize="11" />
                            </Frame>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>

</ContentPage>
